<VisibleHiddenComponent Visible="@Visible">
    @if (Key is null)
    {
        @ChildContent
    }
    else
    {
        <RefreshComponent 
                      CanRender="@CanRender"
                      Key="@Key">
            @ChildContent
        </RefreshComponent>
    }
</VisibleHiddenComponent>